console.log(`
    
    3.5.10 赋值操作符
        
        简单赋值用等于号（=）表示，将右手便的值赋给左手边的变量
        复合赋值使用乘性、加性或位操作符后面跟等于号（=）表示

        · 乘后赋值（*=）
        · 除后赋值（/=）
        · 取模后赋值（ %=）
        · 加后赋值（+=）
        · 减后赋值（-=）
        · 左移后赋值（<<=）
        · 右移后赋值（>>=）
        · 无符号右移赋值（>>>=）

    `);
